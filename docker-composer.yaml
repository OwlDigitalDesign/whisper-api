version: "3.9"

services:
  whisper-api:
    build: .
    container_name: whisper-api
    ports:
      - "8000:8000"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - hf_cache:/app/.cache
    restart: unless-stopped

volumes:
  hf_cache:
